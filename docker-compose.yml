version: '3.8'

services:
  ollama:
    build: .
    container_name: ollama-service
    ports:
      - "11434:11434"
      - "8501:8501"
    volumes:
      - ollama_data:/root/.ollama
    # 如果您的系统有GPU，可以取消下面的注释以启用GPU加速
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # 启动 Ollama 和 Streamlit 服务

volumes:
  ollama_data: